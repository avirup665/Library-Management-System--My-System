import {useEffect,useState} from "react";import api from "../services/api";import Alert from "../components/Alert";
export default function Transactions(){const [tx,setTx]=useState([]),[error,setError]=useState(""),[success,setSuccess]=useState("");const load=async()=>{try{setTx((await api.get("/transactions")).data)}catch(e){setError(e.response?.data?.message||"Could not load transactions")}};useEffect(()=>{load()},[]);const ret=async id=>{if(!confirm("Mark this book as returned?"))return;try{setError("");await api.put(`/transactions/${id}/return`);setSuccess("Book returned successfully");await load()}catch(e){setError(e.response?.data?.message||"Could not return book")}};return <div><div className="page-heading"><div><h2>Transactions</h2><p>View issue history and return issued books</p></div></div><Alert message={error}/><Alert type="success" message={success}/><section className="panel"><div className="panel-header"><h3>Issue / Return History</h3><span>{tx.length} transactions</span></div>{tx.length===0?<div className="empty-state">No transactions found.</div>:<div className="table-wrap"><table><thead><tr><th>Book</th><th>Member</th><th>Issue Date</th><th>Return Date</th><th>Status</th><th>Action</th></tr></thead><tbody>{tx.map(t=><tr key={t._id}><td>{t.bookId?.title||"Deleted book"}</td><td>{t.memberId?.name||"Deleted member"}</td><td>{new Date(t.issueDate).toLocaleDateString()}</td><td>{t.returnDate?new Date(t.returnDate).toLocaleDateString():"-"}</td><td><span className={`badge ${t.status.toLowerCase()}`}>{t.status}</span></td><td>{t.status==="Issued"?<button className="btn small primary" onClick={()=>ret(t._id)}>Return Book</button>:<span className="muted">Completed</span>}</td></tr>)}</tbody></table></div>}</section></div>}
